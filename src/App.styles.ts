import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    maxWidth: 800,
    minHeight: '100%',
    gap: 2
  },
  column: {
    flex: 1,
    height: '100%'
  }
})
